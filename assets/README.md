# Portfolio Assets

Add the final media files here before publishing GitHub Pages:

- `final_demo.mp4` - final edited project video.
- `video_poster.svg` - still image shown before the video plays.
- `tensorboard_reward.svg` - exported TensorBoard PPO reward curve.
- `yolo_confusion_matrix.svg` - Ultralytics YOLO confusion matrix image.

The portfolio page references these exact filenames from `index.html`.
